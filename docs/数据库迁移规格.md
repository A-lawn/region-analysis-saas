# 数据库迁移完整规格

## 迁移文件清单 (database/migrations/)

008_extend_industry_models.sql
ALTER TABLE site_optimization_models ADD COLUMN analysis_params JSONB DEFAULT '{}';
ALTER TABLE site_optimization_models ADD COLUMN decision_thresholds JSONB DEFAULT '{}';
ALTER TABLE site_optimization_models ADD COLUMN benchbarks JSONB DEFAULT '{}';
ALTER TABLE site_optimization_models ADD COLUMN kpi_weights JSONB DEFAULT '{}';
ALTER TABLE site_optimization_models ADD COLUMN sort_order INTEGER DEFAULT 0;

009_industry_keywords.sql
CREATE TABLE IF NOT EXISTS industry_keywords (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  industry VARCHAR(50) NOT NULL REFERENCES site_optimization_models(industry) ON DELETE CASCADE,
  keyword VARCHAR(50) NOT NULL,
  priority INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(industry, keyword)
);
预置48行: 12行业x4关键词

010_analysis_types.sql
CREATE TABLE IF NOT EXISTS analysis_types (
  type VARCHAR(50) PRIMARY KEY,
  display_name VARCHAR(100) NOT NULL,
  enabled BOOLEAN NOT NULL DEFAULT true,
  default_params JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
预置: coverage/heatmap/cluster/site-optimization/voronoi/h3-hexagon

011_new_industries.sql
INSERT INTO site_optimization_models (industry, display_name, weights, analysis_params, decision_thresholds, benchbarks, kpi_weights, description, is_default, radius_meters) VALUES
('beverage', '茶饮/咖啡', ...),
('fresh_grocery', '生鲜超市', ...),
('hotel', '酒店', ...),
('medical_aesthetics', '医美/口腔', ...),
('education', '教育培训', ...),
('pet_service', '宠物服务', ...),
('logistics', '物流/快递驿站', ...)
ON CONFLICT (industry) DO UPDATE ...

012_industry_benchmarks.sql
UPDATE site_optimization_models SET benchbarks = ... WHERE industry = ...;
预置12行业benchmark数据(coverageRatio/avgNeighborDist/cannibalizationIndex等行业中位数)

013_poi_categories.sql
扩展 public_poi category 可选项(非破坏性)
新增POI采集类别: hotel/school/training/parking/pet/beauty/beverage

014_log_retention.sql
与日志相关(如需要DB存储日志则在API日志表加清理策略)

015_seed_kpi_registry.sql
预置55个KPI的计算参数到 kpi_category_map 扩展字段
